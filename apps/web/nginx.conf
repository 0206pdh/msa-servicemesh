server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://benchmark-gateway:8080;
    proxy_set_header X-Correlation-Id $http_x_correlation_id;
    proxy_set_header Host $host;
  }

  location / {
    try_files $uri /index.html;
  }
}
